---
import Head from '@components/Head.astro'
import Header from '@components/Header.astro'
import Footer from '@components/Footer.astro'
import Scripts from '@components/Scripts.astro'
import BoostedScript from "@components/BoostedScript.astro"
import { getConfig } from '@libs/config'

interface Props {
  data: {
    title?: string
  }
  removeBoosted?: boolean
}

const { data, removeBoosted } = Astro.props
const libVersion = getConfig().lib_version
const echartsVersion = getConfig().echarts_version
const docsVersion = getConfig().docs_version
---

<!DOCTYPE html>
<html lang="en" data-ods-charts-version={libVersion} data-ods-charts-docs-version={docsVersion} data-echarts-version={echartsVersion} >
  <head>
    <Head title={data.title || 'ODS Charts'} />
    <slot name="head-scripts" />
  </head>
  <body>
    <Header title={data.title} />

    <main>
      <slot />
    </main>

    <Footer />
    {!removeBoosted && <BoostedScript />}
    <Scripts />
    <slot name="scripts" />
  </body>
</html>